name: OrderCreated
title: Order Created
summary: Emitted when a new order is persisted in draft status.
contentType: application/json
traits:
  - $ref: '../../../async-common/message-traits/common.yaml#/auditableEvent'
x-message-category: event
x-label:
  entity: Order
  action: Created
x-version:
  current: 1
  status: stable
x-partition-key:
  property: orderId
  scope: aggregate
payload:
  type: object
  required: [orderId, after]
  properties:
    orderId:
      type: string
      format: uuid
    after:
      $ref: '../events/OrderSnapshot.yaml'
