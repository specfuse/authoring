type: object
description: Frozen snapshot of an OrderLine at the moment of an event.
required: [id, orderId, description, quantity, unitPriceMinor]
properties:
  id:
    type: string
    format: uuid
  orderId:
    type: string
    format: uuid
  description:
    type: string
  quantity:
    type: integer
  unitPriceMinor:
    type: integer
