action: send
channel:
  $ref: '../../../async-common/channels/application-events.yaml'
tags:
  - name: Order
summary: Publish Order.Created event
description: |
  Publishing declaration for Order.Created. Wired by the generator into the
  createOrder REST operation (which declares x-emits: Order.Created).
messages:
  - $ref: '../messages/OrderCreated.yaml'
