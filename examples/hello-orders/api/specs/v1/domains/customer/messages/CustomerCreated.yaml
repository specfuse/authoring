name: CustomerCreated
title: Customer Created
summary: Emitted when a new customer is persisted.
contentType: application/json
traits:
  - $ref: '../../../async-common/message-traits/common.yaml#/auditableEvent'
x-message-category: event
x-label:
  entity: Customer
  action: Created
x-version:
  current: 1
  status: stable
x-partition-key:
  property: customerId
  scope: aggregate
payload:
  type: object
  required: [customerId, after]
  properties:
    customerId:
      type: string
      format: uuid
    after:
      $ref: '../events/CustomerSnapshot.yaml'
