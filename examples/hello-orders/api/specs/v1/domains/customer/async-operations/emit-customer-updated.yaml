action: send
channel:
  $ref: '../../../async-common/channels/application-events.yaml'
tags:
  - name: Customer
summary: Publish Customer.Updated event
description: |
  Publishing declaration for Customer.Updated. Wired by the generator into the
  updateCustomer REST operation (which declares x-emits: Customer.Updated).
messages:
  - $ref: '../messages/CustomerUpdated.yaml'
