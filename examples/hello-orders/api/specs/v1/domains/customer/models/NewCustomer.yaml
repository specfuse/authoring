type: object
description: Request body for creating a Customer. Client-settable fields only.
required: [firstName, lastName]
properties:
  firstName:
    type: string
    x-sample:
      provider: faker
      path: person.firstName
  lastName:
    type: string
    x-sample:
      provider: faker
      path: person.lastName
  email:
    type: string
    format: email
    x-sample:
      provider: faker
      path: internet.email
