# =============================================================================
# Project enums shared across OpenAPI operations.
# =============================================================================
# Authoritative source for the project's closed Role enum, referenced by every
# x-roles declaration in this project.

# -----------------------------------------------------------------------------
# Role — hello-orders' closed role enum.
# -----------------------------------------------------------------------------
Role:
  type: string
  x-enum-case: PascalCase
  enum:
    - Admin           # System administrator — full access
    - Manager         # Tenant-level manager
    - Customer        # End-user with self-service access
    - Authenticated   # Pre-business-role flows (not used by any operation in this example)
