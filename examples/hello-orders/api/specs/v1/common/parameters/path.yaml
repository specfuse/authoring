# =============================================================================
# Path parameters shared across operations in hello-orders.
# =============================================================================

tenantId:
  name: tenantId
  in: path
  required: true
  schema:
    type: string
    format: uuid

customerId:
  name: customerId
  in: path
  required: true
  schema:
    type: string
    format: uuid

orderId:
  name: orderId
  in: path
  required: true
  schema:
    type: string
    format: uuid
